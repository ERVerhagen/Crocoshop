{% extends 'base.html.twig' %}

{% block body %}
    <h1>Artikel creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
    <a  href="{{ path('artikel_new') }}"><button type="submit"  class="btn btn-success mb-3"><i class="fas fa-plus-circle"></i> Create new</button></a>
    <a class="btn btn-primary mb-3 float-right" href="{{ path('artikel_index') }}">Back to the list</a>
    {{ form_end(form) }}
{% endblock %}
