{% extends 'base.html.twig' %}

{% block body %}
    <h1>Staffelgroep</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ staffelgroep.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('staffelgroep_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('staffelgroep_edit', { 'id': staffelgroep.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
